1. **修复closeMenu方法**：

   * 调整状态变化顺序，确保动画流畅

   * 避免在状态变化过程中触发不必要的重新渲染

2. **优化expandedMenuOverlay方法**：

   * 修复菜单高度计算，确保在items为空时也能正常工作

   * 替换UIScreen.main.bounds.width为更安全的布局方式

   * 确保视图状态一致

3. **检查并修复可能的无限循环**：

   * 确保isItemSelected方法不会导致无限循环

   * 优化ForEach循环中的视图生成

4. **优化状态管理**：

   * 确保activeFilterType和showMenuContent的状态变化同步

   * 避免在动画过程中触发多次状态更新

具体修改：

* 修改`LibraryView.swift`文件中的`closeMenu`方法

* 优化`expandedMenuOverlay`方法中的布局计算

* 检查并修复`isItemSelected`方法

* 调整状态变化的时序

