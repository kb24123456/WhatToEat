1. **使用navigationDestination替代NavigationLink**：在iOS 16+中，navigationDestination是延迟加载的，只有在用户点击时才会渲染目标视图，避免提前预渲染复杂的RestaurantDetailView
2. **简化RestaurantCard**：移除NavigationLink，改为使用普通View并添加onTapGesture触发导航
3. **在LibraryView中添加navigationDestination**：集中处理导航逻辑，确保延迟加载目标视图
4. **优化RestaurantDetailView**：移除不必要的同步图片加载和复杂初始渲染
5. **添加延迟加载机制**：将复杂操作和数据加载推迟到视图出现后执行

