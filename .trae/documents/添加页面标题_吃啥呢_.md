1. 修改 `LibraryView.swift` 文件中的 `headerSection` 属性
2. 在 `headerSection` 的 HStack 中，在左侧添加标题文本 "吃啥呢"
3. 使用 AppTheme 中定义的字体和颜色，确保标题与应用风格一致
4. 调整布局，使标题左对齐，导入按钮右对齐，保持良好的视觉层次
5. 确保标题与搜索框之间有适当的间距，不影响搜索框的使用

具体修改：

* 将 `headerSection` 中的 HStack 从只有右侧按钮，改为左侧标题 + 右侧按钮的布局

* 使用 `AppTheme.Fonts.title2` 或 `title3` 作为标题字体

* 使用 `AppTheme.Colors.textPrimary` 作为标题颜色

* 保持原有的 padding 和 background 设置

