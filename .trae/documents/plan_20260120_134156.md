1. **分析当前颜色设置**：

   * 地区：当前使用 `AppTheme.Colors.textSecondary` (#948E88，浅棕色)
   * 距离：当前使用 `AppTheme.Colors.textSecondary` (#948E88，浅棕色)
   * 菜系：当前使用 `AppTheme.Colors.textSecondary` (#948E88，浅棕色)
   * 评价：当前使用 `AppTheme.Colors.textSecondary` (#948E88，浅棕色)

2. **目标颜色**：
   * `AppTheme.Colors.textPrimary` (#332E2B，深棕色) - 比textSecondary更深，更突出

3. **修改内容**：
   * 在 `LibraryView.swift` 文件中，修改 `RestaurantCard` 结构体的 `body` 部分
   * 将地区文本的颜色从 `textSecondary` 改为 `textPrimary`
   * 将距离文本的颜色从 `textSecondary` 改为 `textPrimary`
   * 将菜系文本的颜色从 `textSecondary` 改为 `textPrimary`
   * 将评价文本的颜色从 `textSecondary` 改为 `textPrimary`

4. **预期效果**：
   * 地区、距离、菜系、评价的字体颜色加深
   * 视觉上更加突出，比之前更易读
   * 保持与餐厅名称的视觉层级关系（餐厅名称仍是最重要的信息）
   * 整体视觉效果更加清晰，信息层次分明
   * 代码编译成功

5. **验证步骤**：
   * 修改完成后，运行xcodebuild命令验证编译是否成功
   * 确保所有组件颜色符合要求

