# 移除筛选按钮现有逻辑计划

## 1. 移除的核心内容

### 1.1 状态管理移除
- 移除 `activeFilterType` 状态变量
- 移除 `showMenuContent` 状态变量
- 移除 `buttonFrames` 状态变量
- 移除 `menuNamespace` 命名空间

### 1.2 UI组件移除
- 移除 `filterBarSection` 中的筛选胶囊按钮
- 移除沉浸式蒙层和菜单覆盖
- 移除 `filterCapsule` 函数
- 移除 `expandedMenuOverlay` 函数

### 1.3 逻辑与辅助函数移除
- 移除筛选相关的辅助函数：`closeMenu`、`handleSelection`、`getCapsuleTitle`、`checkIsSelected`、`isItemSelected`、`getItems`
- 移除偏好设置监听
- 移除 `FilterType` 枚举、`SortOption` 枚举和 `FilterPositionKey` 结构

## 2. 保留的内容
- 基本的搜索功能
- 餐厅列表展示
- RestaurantCard 组件
- 导入功能

## 3. 移除步骤

### 步骤1：移除状态变量
- 移除第19-23行的筛选相关状态变量

### 步骤2：移除UI组件和覆盖层
- 移除第65-77行的沉浸式蒙层和菜单覆盖
- 简化 `filterBarSection`，仅保留搜索相关功能（如果需要）

### 步骤3：移除函数和逻辑
- 移除 `filterCapsule` 函数（第171-210行）
- 移除 `expandedMenuOverlay` 函数（第214-273行）
- 移除筛选相关的辅助函数（第276-318行）

### 步骤4：移除枚举和结构
- 移除 `FilterType` 枚举（第423行）
- 移除 `SortOption` 枚举（第424-427行）
- 移除 `FilterPositionKey` 结构（第428-431行）

### 步骤5：移除偏好设置监听
- 移除第80-82行的偏好设置监听

### 步骤6：简化筛选逻辑
- 简化 `filteredAndSortedRestaurants` 计算属性，仅保留搜索功能

## 4. 预期结果
- 保留基本的搜索和餐厅列表功能
- 移除所有与筛选按钮相关的复杂动画和交互逻辑
- 代码结构更简洁，便于用户进行筛选功能的重构
- 确保项目能够正常编译和运行

## 5. 注意事项
- 确保移除后不会影响其他功能的正常运行
- 保留必要的代码结构，便于用户后续重构
- 移除所有相关的导入语句（如果有）
- 确保RestaurantCard组件能够正常显示