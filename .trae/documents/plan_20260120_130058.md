1. **分析当前字体设置**：

   * 人均消费：当前使用 `AppTheme.Fonts.headline`
   * 地区：当前使用 `AppTheme.Fonts.caption`
   * 距离：当前使用 `AppTheme.Fonts.caption`
   * 菜系：当前使用 `AppTheme.Fonts.caption`
   * 评价：当前使用 `AppTheme.Fonts.caption`

2. **修改方案**：

   * 将人均消费的字体样式从 `headline` 改为 `subheadline` (15pt左右)
   * 将地区的字体样式从 `caption` 改为 `subheadline` (15pt左右)
   * 将距离的字体样式从 `caption` 改为 `subheadline` (15pt左右)
   * 将菜系的字体样式从 `caption` 改为 `footnote` (13pt左右)
   * 将评价的字体样式从 `caption` 改为 `subheadline` (15pt左右)

3. **修改内容**：

   * 在 `LibraryView.swift` 文件中，修改 `RestaurantCard` 结构体的 `body` 部分
   * 保持字体颜色不变，仅修改字体样式
   * 确保所有修改都使用 AppTheme.Fonts 中定义的样式

4. **预期效果**：

   * 人均消费、地区、距离、评价使用 15pt 左右的 subheadline 样式
   * 菜系使用 13pt 左右的 footnote 样式
   * 整体视觉效果更加清晰，信息层次分明
   * 代码编译成功

5. **验证步骤**：

   * 修改完成后，运行xcodebuild命令验证编译是否成功
   * 确保所有组件字体大小符合要求

