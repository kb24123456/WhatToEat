### 1. 实现导航条添加按钮的点击事件处理
- 在ContentView.swift中添加状态变量`@State private var showAddRestaurant = false`
- 修改添加按钮的点击事件，将`print("Add button tapped")`替换为`showAddRestaurant = true`
- 在ContentView中添加sheet修饰符，显示AddRestaurantView：`.sheet(isPresented: $showAddRestaurant) { AddRestaurantView() }`

### 2. 调整添加界面的设计风格
- **视觉风格**：将AddRestaurantView从Form风格改为与LibraryView一致的卡片式设计
- **色彩方案**：使用AppTheme.Colors中定义的颜色，确保与餐厅列表页一致
- **字体选择**：使用AppTheme.Fonts中定义的字体，保持字体风格统一
- **组件样式**：统一按钮、输入框、卡片的圆角、阴影等样式

### 3. 实现一致的交互体验
- 添加与餐厅列表页一致的动画效果
- 实现平滑的过渡效果
- 添加操作反馈，如按钮点击反馈、保存成功提示

### 4. 完善表单元素和验证逻辑
- 确保所有必要的表单字段都已包含
- 添加表单验证逻辑，如必填字段验证
- 实现表单提交功能，将新餐厅保存到数据库

### 5. 确保响应式设计
- 调整布局，确保在不同屏幕尺寸下都能正确显示
- 使用AppTheme中定义的间距和尺寸常量
- 确保组件能够自适应不同屏幕尺寸

### 6. 保持视觉层次和信息架构一致性
- 确保添加界面的信息架构与餐厅列表页一致
- 保持相同的视觉层次，使用相同的标题、副标题样式
- 确保操作流程符合用户预期

### 具体修改步骤
1. 修改ContentView.swift，实现添加按钮的点击事件和sheet显示
2. 重构AddRestaurantView.swift，采用卡片式设计
3. 使用AppTheme中定义的样式常量，确保设计一致性
4. 添加表单验证和提交逻辑
5. 实现一致的交互效果
6. 测试在不同屏幕尺寸下的表现
7. 重新编译应用，验证修改成功

### 预期结果
修复后，点击底部导航栏的添加按钮，会显示与餐厅列表页设计风格高度一致的添加界面，用户可以方便地添加新餐厅，且交互体验流畅一致。