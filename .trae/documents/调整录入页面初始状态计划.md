## 调整录入页面初始状态计划

### 1. 初始状态调整
- **品类(category)**：从"火锅"改为空字符串`""`
- **区域(district)**：从"江北"改为空字符串`""`
- **评分(rating)**：从5改为0或1（考虑到评分通常是1-5星，需要在保存时处理）

### 2. 保存逻辑处理
- 确保在保存餐厅数据时，对空值进行合理处理
- 与Restaurant模型的默认值保持一致（品类默认"未分类"，区域默认"其他"，评分默认3）

### 3. 实现步骤
1. 更新AddRestaurantView.swift中的@State变量初始值
2. 确保保存逻辑能正确处理空值情况
3. 测试调整后的界面

### 4. 预期效果
- 录入页面打开时，品类、区域、评分字段均为初始空状态
- 用户需要主动选择或输入这些字段的值
- 保存时能正确处理空值，使用合理的默认值

### 5. 技术要点
- 保持与Restaurant模型的一致性
- 确保UI与数据模型的正确映射
- 提供良好的用户体验，明确提示必填字段