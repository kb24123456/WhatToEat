1. **重新极度简化RestaurantCard组件**：移除所有复杂布局、图片加载和自定义对齐，只保留最基本的餐厅名称和评分显示
2. **移除可能导致无限循环的动画**：删除listSection中的.animation(.spring(), value: filteredAndSortedRestaurants)
3. **检查ImageManager实现**：确保图片加载不会导致阻塞或无限循环
4. **验证初始数据延迟添加机制**：确保数据添加不会与视图渲染冲突

通过这些修改，彻底消除导致应用卡在RestaurantCard.body.getter的无限循环问题