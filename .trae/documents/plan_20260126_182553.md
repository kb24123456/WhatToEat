# RestaurantDetailView 功能增强

## 任务1：添加打卡功能按钮

在封面图下方、人均消费卡片上方添加浮动打卡按钮：

```swift
// heroSection 后添加
checkInButtonSection

// 样式设计
HStack {
    Spacer()
    Button {
        showSheet = true
    } label: {
        HStack(spacing: 8) {
            Image(systemName: "checkmark.circle.fill")
            Text("去打卡")
        }
        .font(.system(size: 15, weight: .semibold))
        .foregroundColor(.white)
        .padding(.horizontal, 24)
        .padding(.vertical, 12)
        .background(
            Capsule()
                .fill(Color(hex: "#FF2442"))
                .shadow(color: Color(hex: "#FF2442").opacity(0.3), radius: 12, x: 0, y: 6)
        )
    }
    .padding(.trailing, AppTheme.Spacing.lg)
    .offset(y: -20) // 稍微覆盖到封面图上
}
```

## 任务2：我的印象与标签二次编辑

### 我的印象卡片
```swift
// 可编辑状态切换
@State private var isEditingReview = false
@State private var editedReview = ""

// 点击进入编辑模式，展开动画
// 保存时带弹簧回弹效果
```

### 标签模块
```swift
// 当前显示为只读标签
// 添加"编辑标签"按钮
// 进入编辑模式：标签变为可删除（X 按钮），底部添加添加标签输入框
// 动画：标签淡入淡出、输入框滑动进入
```

### 动画效果
- 展开/收起：`.spring(response: 0.4, dampingFraction: 0.8)`
- 标签删除：缩小淡出动画
- 标签添加：弹簧放大动画

## 任务3：地区与品类信息（人均下方）

### 新增信息展示区
```swift
// 在 statsCardSection 后添加
infoSection: VStack(alignment: .leading, spacing: 8) {
    HStack {
        Label(restaurant.district, systemImage: "mappin.circle.fill")
        Label(restaurant.category, systemImage: "tag.fill")
    }
    
    // 编辑模式：显示 Picker 或 TextField
}

// 点击进入编辑模式，支持修改 district 和 category
```

### 动画
- 信息卡片滑入效果
- 编辑状态切换弹簧动画

## 改动文件
`RestaurantDetailView.swift` - 添加状态变量和组件方法