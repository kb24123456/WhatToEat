# 改回完整页面显示餐厅详情

## 当前状态
- LibraryView 使用 `CenteredDetailCardView` 作为叠加层（overlay）
- `RestaurantDetailView` 已存在完整的页面实现

## 改动方案

### 1. 修改 LibraryView 导航方式
将卡片叠加层改为全屏页面导航：

```swift
// 方案 A：使用 NavigationStack + NavigationLink
NavigationStack {
    ZStack { ... }
    .navigationDestination(for: Restaurant.self) { restaurant in
        RestaurantDetailView(...)
    }
}

// 方案 B：使用 fullScreenCover（保留动画效果）
.fullScreenCover(isPresented: $isDetailPresented) {
    RestaurantDetailView(
        restaurant: selectedRestaurant!,
        isPresented: $isDetailPresented,
        onDismiss: { selectedRestaurant = nil }
    )
}
```

### 2. 修改 CenteredDetailCardView（可选）
- 删除或保留作为备用方案
- 移除 LibraryView 中的引用

### 3. 保留的功能
- 展开/收起动画（通过 fullScreenCover 实现）
- 打卡功能（CheckInView）
- 照片管理（多图支持）

## 改动文件
1. `LibraryView.swift` - 修改导航逻辑
2. `RestaurantDetailView.swift` - 确保完整性（可能需要适配）