1. **分析需求**：
   - 互换菜系与评分的位置
   - 将菜系信息从TagView样式改为普通文字显示，与地区信息样式一致
   - 保持评分的TagView样式不变

2. **修改内容**：
   - 在`LibraryView.swift`文件中，修改`RestaurantCard`结构体的`body`部分
   - 修改第三行HStack的内容，调整菜系和评分的顺序
   - 将菜系信息从`TagView`改为普通`Text`组件
   - 为菜系文字应用与地区信息一致的样式：`font(AppTheme.Fonts.caption).foregroundColor(.gray)`
   - 调整组件间的间距

3. **具体修改**：
   - 将第三行的HStack内容从：
     ```swift
     HStack(spacing: 6) {
         TagView(text: restaurant.type, color: AppTheme.Colors.primary)
         TagView(text: "⭐️ \(restaurant.rating)", color: AppTheme.Colors.secondary)
     }
     ```
   - 修改为：
     ```swift
     HStack(spacing: 12) {
         TagView(text: "⭐️ \(restaurant.rating)", color: AppTheme.Colors.secondary)
         Text(restaurant.type)
             .font(AppTheme.Fonts.caption)
             .foregroundColor(.gray)
     }
     ```

4. **预期效果**：
   - 第三行显示：评分（TagView样式） + 菜系（普通文字样式）
   - 菜系文字样式与地区信息一致
   - 整体布局保持清晰的信息层次
   - 代码编译成功

5. **验证步骤**：
   - 修改完成后，运行xcodebuild命令验证编译是否成功
   - 确保所有组件位置和样式符合要求

