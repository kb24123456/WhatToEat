1. **当前颜色分析**：

   * 餐厅名称：使用 `AppTheme.Colors.textPrimary` (#332E2B，深棕色) - 最高层级
   * 人均消费：使用 `AppTheme.Colors.accent` (#FF2442，强调色) - 强调层级
   * 地区：使用 `.gray` (SwiftUI默认灰色，较浅)
   * 距离：使用 `.gray` (SwiftUI默认灰色，较浅)
   * 菜系：使用 `.gray` (SwiftUI默认灰色，较浅)
   * 评价：使用 `.secondary` (SwiftUI默认次要颜色，较浅)

2. **AppTheme颜色定义**：

   * `textPrimary`: #332E2B (深棕色) - 主要文本颜色
   * `textSecondary`: #948E88 (浅棕色) - 次要文本颜色，比默认灰色深，但比textPrimary浅

3. **解决方案**：

   * 将地区、距离、菜系、评价组件的颜色统一修改为 `AppTheme.Colors.textSecondary`
   * 这个颜色比当前的灰色深，同时比餐厅名称的textPrimary浅，符合用户要求的层级关系
   * 保持人均消费的强调色不变

4. **修改内容**：

   * 在 `LibraryView.swift` 文件中，修改 `RestaurantCard` 结构体的 `body` 部分
   * 将地区文本的颜色从 `.gray` 改为 `AppTheme.Colors.textSecondary`
   * 将距离文本的颜色从 `.gray` 改为 `AppTheme.Colors.textSecondary`
   * 将菜系文本的颜色从 `.gray` 改为 `AppTheme.Colors.textSecondary`
   * 将评价文本的颜色从 `.secondary` 改为 `AppTheme.Colors.textSecondary`

5. **预期效果**：

   * 地区、距离、菜系、评价组件的颜色加深
   * 颜色层级：餐厅名称 > 人均消费 > 地区/距离/菜系/评价
   * 整体视觉效果更加清晰，信息层次分明
   * 代码编译成功

6. **验证步骤**：

   * 修改完成后，运行xcodebuild命令验证编译是否成功
   * 确保所有组件颜色符合要求

