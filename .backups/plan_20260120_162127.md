## 代码回滚计划

### 目标状态

将项目代码恢复到2026-01-20 13:41:56的状态，此时已完成餐厅卡片文本颜色修改，具体表现为：

* 餐厅卡片中地区、距离、菜系、评价文本颜色从`textSecondary`改为`textPrimary`

* 保持其他功能正常运行

* 确保应用不会出现闪退问题

### 回滚策略

1. **查看当前修改详情**：检查所有修改文件的具体变更内容
2. **选择性回滚**：保留餐厅卡片文本颜色修改，回滚其他不必要的变更
3. **验证回滚结果**：确保回滚后代码符合目标状态

### 具体步骤

#### 1. 检查当前修改详情

```bash
git diff WhatToEat/LibraryView.swift WhatToEat/ContentView.swift WhatToEat/ImportDataView.swift WhatToEat/AppTheme.swift WhatToEat/UIComponents.swift
```

#### 2. 回滚特定文件修改

* **保留修改**：`LibraryView.swift`中的餐厅卡片文本颜色修改

* **回滚内容**：

  * 删除LibraryView\.swift中的`.alignmentGuide(.top) { _ in -8 }`（导致闪退的代码）

  * 恢复ImportDataView\.swift到原始状态

  * 恢复ContentView\.swift到原始状态

  * 恢复AppTheme.swift到原始状态

  * 恢复UIComponents.swift到原始状态

#### 3. 验证回滚结果

* 确认LibraryView\.swift中餐厅卡片文本颜色为`textPrimary`

* 确认没有导致闪退的代码

* 运行应用验证功能正常

### 预期结果

* 餐厅卡片文本颜色正确显示为`textPrimary`

* 应用启动正常，没有闪退

* 其他功能保持正常

### 风险评估

* **数据丢失风险**：低，因为我们只回滚特定的代码修改，不会影响数据

* **功能影响风险**：低，回滚操作经过精心选择，只移除有问题的代码

* **编译错误风险**：低，所有回滚操作都会经过编译验证

