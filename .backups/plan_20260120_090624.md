## 餐厅卡片间距修改计划

### 问题分析
根据用户需求，需要调整餐厅卡片的间距设置：
1. 卡片与页面边缘的水平间距应为16pt（最小水平间距）
2. 卡片内组件应取消与卡片边缘的水平最小间距，直接贴合卡片边缘

### 现状检查
通过代码分析，当前状态如下：
- **卡片与页面边缘间距**：已通过 `.padding(.horizontal, AppTheme.Spacing.lg)` 设置为16pt（位于listSection的RestaurantCard调用处）
- **卡片内组件间距**：卡片内部的HStack没有应用水平间距，内组件已直接贴合卡片边缘

### 解决方案
尽管当前设置已基本符合要求，但需要进行最终确认和调整，确保完全符合用户需求：

1. **保持卡片与页面边缘的间距**：确认并保持 `listSection` 中 `RestaurantCard` 的 `.padding(.horizontal, AppTheme.Spacing.lg)` 设置
2. **确认卡片内组件无间距**：确保卡片内部的HStack没有应用任何水平间距，内组件直接贴合卡片边缘
3. **验证修改效果**：确保修改后卡片布局符合预期，无额外间距限制

### 具体修改步骤
1. 打开 `LibraryView.swift` 文件
2. 确认 `listSection` 中 `RestaurantCard` 的水平间距设置为 `AppTheme.Spacing.lg`（16pt）
3. 确认 `RestaurantCard` 内部的HStack没有应用任何水平间距
4. 如有必要，调整相关代码以确保符合要求
5. 验证修改后的布局效果

### 预期效果
- 卡片与页面边缘的水平间距保持16pt
- 卡片内组件直接贴合卡片边缘，无水平最小间距限制
- 卡片整体布局美观，组件对齐方式保持不变
- 卡片的交互功能和视觉效果不受影响

### 注意事项
- 确保只修改相关的间距设置，不影响其他样式和功能
- 保持卡片内部组件的垂直间距和对齐方式不变
- 确保修改后不会影响其他使用 `RestaurantCard` 组件的地方