import Foundation

// MARK: - 后端代理配置模板
// 使用说明：
// 1. 复制此文件为 AppConfig.swift
// 2. 填入你的应用后端代理地址
// 3. 确保 AppConfig.swift 已被添加到 .gitignore，不要提交到 Git
//
// ⚠️ 注意：此文件仅作为模板参考，不会被编译到项目中
// 实际使用的是 AppConfig.swift 中的 AppEnvironment / AIConfig

/*
enum AppEnvironment {
    static let backendBaseURL = URL(string: "https://api.example.com")
}

struct AIConfig {
    static let generateFortuneURL = AppEnvironment.backendBaseURL?
        .appending(path: "v1/food-fortune/generate")
}
*/
